# Vite proxy target (used in dev server — see vite.config.js)
VITE_API_TARGET=http://running.wsk-skill53.link

# Set this to the full base URL when NOT using the Vite proxy (e.g. production)
# VITE_API_BASE_URL=https://running.wsk-skill53.link/v1

# Default user ID loaded on startup
VITE_DEFAULT_USER_ID=U123
